version: '3.8'

# for deployment

services:
  app:
    build:
      context: .
      dockerfile: TranscEND.Dockerfile
    volumes:
      - ${PWD}:/app/
    ports:
      - 8000:8000
    # restart: unless-stopped
    entrypoint: /app/start.sh